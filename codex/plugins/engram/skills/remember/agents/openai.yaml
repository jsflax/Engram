interface:
  display_name: "Engram Remember"
  short_description: "Save useful knowledge for future sessions"

dependencies:
  tools:
    - type: "mcp"
      value: "memory"
      description: "Engram memory MCP server"
