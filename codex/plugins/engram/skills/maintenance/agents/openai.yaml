interface:
  display_name: "Engram Maintenance"
  short_description: "Consolidate and organize selected memories"

dependencies:
  tools:
    - type: "mcp"
      value: "memory"
      description: "Engram memory MCP server"

policy:
  allow_implicit_invocation: false
