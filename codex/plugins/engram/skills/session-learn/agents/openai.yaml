interface:
  display_name: "Engram Session Learn"
  short_description: "Save durable insights from this session"

dependencies:
  tools:
    - type: "mcp"
      value: "memory"
      description: "Engram memory MCP server"

policy:
  allow_implicit_invocation: false
