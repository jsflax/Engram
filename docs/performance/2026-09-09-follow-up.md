# Bounded follow-up — September 9, 2026

Status: the requested additional pass is complete. On September 9, after the
results and local validation app were handed off, the user explicitly requested
"push and release" for v0.14.4. That authorizes publication with the known
failures below; it does not establish that manual validation passed. The
remaining costs are better attributed, not fully resolved. This supplements the
[original performance report](2026-09-08.md), without replacing its failed runs.

Release decision: proceed with the documented 34.583 ms frame p95, ten menu
samples above 100 ms, the unchanged model-assessment test failure in the latest
complete local suite, and the outstanding final-cache memory soak. Test
assertions and general performance gates remain unchanged. Signing,
notarization, publication, and updater verification belong to the subsequent
release workflow, not to this investigation's validation evidence.

## Prespecified scope

1. One bounded, five-second Time Profiler capture during a real-copy Graph tab
   transition, using the existing Xcode candidate. Profiler-contended UI timings
   are diagnostic only, not release-gate measurements.
2. One same-binary nebula **on / off / on** comparison: the same isolated
   31,674-memory copy, seeded outer orbit, 3,600 recorded-frame target, and 1,500
   active warmup frames. Disabling nebulae changes visuals, so the off run cannot
   satisfy any release gate. Its temporary switch is marked in the CSV header.
3. One candidate with equality guards on redundant nebula entity-position
   writes. No particle count, lifetime, size, visibility, or drawing-budget
   reduction. Measure normal rendering and real-copy menus with all effects on.
4. Split menu latency into event-to-recorder-begin, configuration mutation,
   and post-mutation-to-draw phases in a separate diagnostic CSV. Preserve the
   original event timestamp, three-column response CSV, and 100 ms gate.

No repeat-until-pass runs, release-gate waivers, or changes to the unrelated
model-assessment test are part of this pass. The final-cache memory soak from
the original report remains outstanding.

## Findings before measurements

- No leaked/duplicate nebula entities were found in the keyed lifecycle.
- Project nebulae were still visible at the prior trace's approximately 4,200
  unit camera distance; the minimum far-fade endpoint is 5,200. Pausing hidden
  emitters alone therefore cannot be presumed to remove the dominant VFX work.
- Existing nebula entities received identical position assignments every frame
  even when their cached centroids were unchanged. Equality guards avoid those
  writes while still checking actual entity state, including external changes.
- The copied database has the expected audit lookup index on
  `(tableName, rowId, operation)`. Selected-tab writes use one-row SQL updates
  with table/column-scoped invalidation; no full Memory/Edge scan or global
  observer broadcast was found in that path.
- The current response metric includes time before the button action's recorder
  begins. Phase diagnostics are needed before attributing the entire delay to
  SwiftUI layout or the SQLite setter.

## Targeted Graph-tab CPU capture

The single five-second Time Profiler capture completed without reaching its
safety limits: recorder/process-tree peak RSS was approximately 711 MiB and the
trace was approximately 90 MiB. The existing UI diagnostic finished and all
owned processes exited. Artifact: `graph-tab-cpu-diagnostic`.

The Graph synthesize-event-to-idle window covers 530 ms, not an exact first-draw
interval. It contains 195 ms of main-thread running sample weight:

- 125 ms in `NSHostingView.layout` / SwiftUI render work, including 83 ms in
  `AccessibilityNode.updateFocusResponder`, 81 ms in focus responder traversal,
  and 52 ms in Swift protocol-conformance lookup. These are overlapping inclusive
  weights and must not be added together.
- 57 ms in Engram's renderer and about 1 ms in SQL/Lattice frames.
- The preceding 500 ms contains 137 ms of main-running samples, including 72 ms
  in XCTest accessibility hierarchy work and 64 ms in the renderer. The next
  500 ms contains 83 ms total, including 81 ms in the renderer.

This identifies substantial SwiftUI accessibility/focus work under automation;
it is not evidence for another graph-wide SQL optimization. Running samples do
not measure blocked waits, and the trace does not establish that manual
interaction without XCTest meets the 100 ms target. Accessibility remains
intact. The upcoming phase sidecar preserves and supplements the original gate.

The temporary nebula-disable source switch has already been removed from the
working candidate. Only the separately staged diagnostic binary contains it;
the three-case comparison uses that same binary throughout.

## Nebula on / off / on comparison

All three serial runs completed with 3,599 recorded frames, all 31,674 memories,
and 159,730 graph edges. The same staged binary, seed, camera, fixture, and
1,500-active-frame warmup were used. Artifacts: `last-pass-nebula-on1`,
`last-pass-nebula-off`, and `last-pass-nebula-on2`.

| Nebula effects | Frame interval p95 | Engram scene CPU p95 | Duration |
| --- | ---: | ---: | ---: |
| On, first run | 34.038 ms | 5.124 ms | 97.703 s |
| Off, diagnostic only | 18.120 ms | 5.092 ms | 63.607 s |
| On, repeat | 33.931 ms | 5.173 ms | 95.922 s |

The substantial cadence improvement reverses when effects return, while measured
scene CPU is nearly unchanged. Nebula effects materially contribute to the
remaining interval miss outside Engram's measured scene callback. This comparison
does not separate native particle simulation CPU from GPU rendering/fill cost.
The earlier VFX-heavy CPU profile is supporting evidence, not a GPU measurement.
The off run changes visuals and **does not clear the release gate**. No emitter
budget was reduced and no particle-disable switch remains in the candidate.

## Candidate correctness

- Normal SwiftPM Release app build passed in 35.16 seconds.
- Xcode Release app/test build passed; version 0.14.4/build 13 was supplied on
  the command line only, without editing the project's version settings.
- All 55 hosted tests passed, including both new timing-decomposition/CSV tests.
- All five parser tests and the 128-project scrolling regression passed.
- Seven isolated Release nebula tests passed against copied current source,
  including the new transform regression. The first compile exposed a Swift 6
  non-Sendable test capture; capturing the two scalar expected values fixed the
  test without changing its assertions or production code. Both logs are kept.
- The prior complete package run's unchanged model-assessment failure remains
  recorded; this pass did not retry or alter that assertion.

## Manual validation focus

Use the separate local validation app after quitting the installed Engram. It
is an optimized Release test build, not Developer ID-signed or notarized; it is not a
published updater release and does not replace `/Applications/Engram.app`.

Local app: `.build/validation-0.14.4-13.b3ILGg/Engram.app` (0.14.4, build 13).
It has the linker's ad-hoc executable signature, not a sealed distribution
signature. The copy was made from the same Xcode product used for the final UI
test. No diagnostic databases, CSVs, or screenshots were packaged with it.

Check the large real graph with nebulae visible while orbiting/zooming, repeated
sidebar/tab/feed openings, project/relation scrolling and toggles, and the
signed-in Account view (not covered by the isolated menu fixture). The remaining
30-minute final-cache memory soak is still outstanding. These were the manual
validation checks proposed at handoff; the later publication instruction is
recorded above. Preparing this app did not clear the automated failures.

## Final normal-effects candidate results

The single candidate renderer run (`last-pass-candidate-real-orbit`) retains all
effects and drawing budgets. It records 3,599 frames in 99.695 seconds and
analyzes 1,857 warmed active frames at the full real-copy graph size. Frame p95
is **34.583 ms**, still failing 33 ms. Scene CPU p95 is 5.054 ms and the maximum
including startup is 77.440 ms, passing the 100 ms CPU budget. Nebula-update CPU
p95 decreased from 0.181–0.189 ms in the on/on controls to 0.099 ms with the
transform guards. This is a small reduction in preparation work, not a measured
frame-cadence fix; the latest frame value is retained rather than replaced by
the lower controls.

The single final real-copy UI run (`last-pass-real`) completes every interaction
and keeps all 27 event-origin response samples and 27 phase rows. Every original
response reconciles with its phases within 0.001 ms. Ten samples exceed 100 ms.
Observed full-count readiness is 10.181 s and all five feed cycles satisfy the
20-row check.

| Action | Original event-to-draw maximum |
| --- | ---: |
| Sidebar open | **168.375 ms** |
| Logs | 69.830 ms |
| Settings | 82.904 ms |
| Account | **109.169 ms** |
| Graph | **153.566 ms** |
| Feed | **125.092 ms** |

All five Graph draws take 129.656–153.566 ms. Their settings mutation takes only
0.431–1.266 ms; mutation-return-to-draw is consistently 121.134–124.712 ms
(mean 122.728 ms). Combined with the CPU capture, this points to post-mutation
SwiftUI accessibility/focus/layout work under automation, not the settings SQL
write. It does not prove that manual interaction without XCTest passes.

Cold Account takes 28.237 ms before recorder begin, 0.440 ms for the mutation,
and 80.492 ms afterward; later post-mutation draws take 22.622–23.543 ms.
Reopened feed samples spend 87.483–93.258 ms before the visibility recorder
begins and 1.136–33.836 ms afterward. Those samples do not establish a slow
latest-20 database read. Recorder begin follows existing guards/callbacks, and
the endpoint remains `NSView.draw`, not compositor presentation.

The pass stops here as requested. The safe transform guards and optional
diagnostic phases are retained; the particle-disable experiment is not in
the candidate. No accessibility removal, visual-budget reduction, assertion
weakening, version-file change, commit, tag, installation, or release occurred
during the investigation pass.
All diagnostic processes were stopped; the installed 0.14.3 app was restored.

Candidate executable SHA-256 values:

```text
SwiftPM renderer: 0448c92a8780865a68050c9f60f66febe529cdae58141f79a17e23668d392a5e
Xcode local app:  5e8db8564cc924b0bdfb2e4f48155f44891a95c8266204be81253b3ee2102f6f
```
